- I want to be able to take run id from wandb and add ifeval to it without rerunning the rest, is this possible? I would like to have a generic system where I can run additional evaluations for runs without rerunning the existing evaluations (unless I specify explicitely that I want to rerun it).

- Add Arena-Hard-Auto v2.0 prompts as a source, but I want it to be generic: I want to use the same code to eval the dataset prompts and Arena-Hard-Auto v2.0 prompts, so I can use reward models or LLM as a judge for both or mix and match. Basically, I want to be able to specify the list of judges for each prompt source and the code would generate replies for each prompt and then judge the responses against either chosen or baseline responses, using style controls. For Arena-Hard-Auto I would like to stay close to the original evaluation or reuse it if possible. But I also want to be able to switch the judge to be a scalar reward model (in this case report raw rewards in addition to win rates) or local LLM as a judge. 